DATABASE_URL=
# Production: use at least 32 random characters (e.g. openssl rand -base64 32)
JWT_SECRET=
PORT=3000
NODE_ENV=development
# Optional: comma-separated origins for browser clients (required for strict CORS in production)
# CORS_ORIGIN=https://your-frontend.railway.app,http://localhost:5173
